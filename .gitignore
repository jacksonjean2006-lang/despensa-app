# Chave de assinatura de release - NUNCA commitar
android/key.properties
*.jks
*.keystore

# Chave PRIVADA do sistema de licença - NUNCA commitar (se vazar, qualquer
# um pode gerar licença válida pro app)
tool/chave_privada.txt

# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
*.iml
